{
  "ConnectionStrings": {
    "Default": "Server=localhost\\SQLEXPRESS;Database=HmsDB;Trusted_Connection=True;TrustServerCertificate=True"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "JwtSettings": {
    "Issuer": "http://localhost:5000",
    "Audience": "http://localhost:5000",
    "Key": "This is my custom Secret key for authnetication which will be moved to be more secured later"
      // TODO Add access and refresh token specific settings
    // Add token expiration settings
  }
}
